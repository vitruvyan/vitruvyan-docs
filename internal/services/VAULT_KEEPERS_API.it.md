# Vault Keepers API

<p class="kb-subtitle">Service LIVELLO 2 per integrity checks, backup/recovery planning e audit trail immutabile.</p>

## Location

- Service: `services/api_vault_keepers/`
- Core puro: `vitruvyan_core/core/governance/vault_keepers/`

## Quick reference

- Doc canonica: `services/api_vault_keepers/README.md`
- Swagger: `/docs`
- Health: `/health` (implementato dal servizio)

## Gruppi endpoint (come in doc)

Vedi `services/api_vault_keepers/README.md`:

- Integrity monitoring (`/integrity/*`)
- Backup operations (`/backup/*`)
- Recovery operations (`/recovery/*`)
- Audit trail (`/audit/*`)

