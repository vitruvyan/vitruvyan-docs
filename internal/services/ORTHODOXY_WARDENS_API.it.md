# Orthodoxy Wardens API

<p class="kb-subtitle">Tribunale epistemico LIVELLO 2: valutazione, classificazione, verdetti e audit trail.</p>

## Location

- Service: `services/api_orthodoxy_wardens/`
- Core puro: `vitruvyan_core/core/governance/orthodoxy_wardens/`

## Quick reference

- Doc canonica: `services/api_orthodoxy_wardens/README.md`
- Swagger: `/docs`
- Health: `/health` (implementato dal servizio)

## Gruppi endpoint (come in doc)

Vedi `services/api_orthodoxy_wardens/README.md`:

- Judgment operations (`/judge/*`)
- Rules management (`/rules/*`)
- Evidence & audit (`/evidence/*`, `/audit/*`)
- Tribunal operations (`/tribunal/*`)

